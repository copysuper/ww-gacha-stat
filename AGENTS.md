# AGENTS.md

## 目标

本项目用于实现**鸣潮抽卡分析**功能。

所有实现、设计、重构、补充功能时，优先参考：

- `doc/reference-gacha-resource-tech-note.md`

该文档定义了抽卡分析的核心数据流、数据结构、合并规则、分析算法、展示字段和图片资源链路。

---

## 当前技术栈

- 前端：`SvelteKit 2` + `Svelte 5` + `TypeScript` + `Vite 6`
- 桌面容器：`Tauri 2`
- 后端：`Rust`

实现时应遵守当前技术栈，不要无故引入与现有栈冲突的框架。

---

## 长期项目原则

### 1. 配置优先

- 能配置就配置，避免把业务规则硬编码到代码里。
- 避免为了小改动重新打包。
- 以下内容优先做成可配置或可扩展：
  - 卡池映射
  - 常驻 5 星 ID 集合
  - 资源仓库源
  - 日志文件路径规则
  - 数据目录路径
  - 调试日志级别

### 2. 高性能

- 抽卡分析核心逻辑优先放在 `Rust`。
- 文件读取、JSON 解析、记录合并、统计分析、资源同步等应尽量在 Rust 侧完成。
- 前端负责展示、交互、状态同步，不承担重计算。
- 避免不必要的全量重复计算；能增量就不要全量。
- 避免 UI 阻塞。

### 3. 现代但稳定

- 尽可能使用当前技术栈中的现代写法。
- 优先选择稳定、维护成本低的方案。
- 不为了“新”而牺牲可读性和可调试性。

### 4. 低耦合

- UI、应用状态、文件存储、网络请求、抽卡分析算法、资源同步应分层。
- 前端不要直接耦合 Rust 内部实现细节。
- 分析算法不要耦合具体 UI 页面。
- 输入方式扩展不能破坏核心分析模块。

### 5. 对扩展开放

- 设计时应优先考虑可扩展性，尽量做到**对扩展开放，对破坏性修改谨慎**。
- 新的输入方式、卡池类型、资源来源、展示方式应尽量通过新增模块、配置或适配层接入，而不是直接改坏现有核心流程。
- 扩展功能应建立在稳定的核心抽卡分析模块之上，避免为了新增一个功能而牵动整条主链路。

### 6. 可读性优先

- 代码简洁易懂，命名清晰。
- Rust 代码要对 Rust 初学者友好。
- 不要过度抽象。
- 小而明确的模块优于巨型全能模块。

### 7. 注释和日志

- 关键逻辑必须有注释，尤其是：
  - 抽卡 URL 提取
  - 请求参数转换
  - 新旧数据合并
  - 5 星/4 星统计
  - 常驻/限定判断
  - 本地资源同步
- 关键路径必须有日志，便于排查问题。
- 日志要说明上下文，不要只打印“失败”。
- 错误信息尽量可读，并包含必要字段。

---

## AI 工作方式要求

### 1. 先读文档再动手

在实现抽卡分析相关功能前，先阅读：

- `AGENTS.md`
- `doc/reference-gacha-resource-tech-note.md`

如果任务是实现路线、模块设计、接口设计，也应先基于这两份文档输出方案。

### 2. 不清楚时先给方案

如果需求较大或会影响整体结构：

- 先给技术实现路线
- 再拆模块
- 最后再开始编码

不要在需求不清时直接大改代码。

### 3. 修改应贴合当前项目

- 不要把项目改造成完全不同的架构。
- 新代码应自然融入当前 `SvelteKit + Tauri + Rust` 结构。
- 优先复用已有目录和配置方式。

### 4. 回答风格

- 使用中文。
- 说明尽量直接、具体、可执行。
- 涉及文件、模块、符号时，使用明确路径和名称。

---

## Rust / Tauri 实现原则

### Rust 侧负责

优先由 Rust 负责的内容：

- 游戏日志解析
- 抽卡 URL 提取
- 请求参数解析与校验
- 抽卡记录网络请求
- 本地 JSON 读写
- 新旧记录合并
- 抽卡分析统计
- 资源同步
- 配置加载

### 前端侧负责

优先由前端负责的内容：

- 页面布局和交互
- 表单输入
- 数据展示
- 图表展示
- 任务状态展示
- 错误提示
- 配置页面

### Tauri 通信要求

- 前后端通信边界清晰。
- Command 输入输出结构要稳定、明确、可序列化。
- 避免把内部临时结构直接暴露给前端。
- 错误返回要统一。

---

## 抽卡分析业务要求

### 1. 核心实现基准

抽卡分析核心逻辑必须以：

- `doc/reference-gacha-resource-tech-note.md`

为准。

如果实现与该文档不同：

- 必须明确说明差异
- 必须说明原因
- 不要无提示地改变统计口径

### 2. 支持的输入来源

当前至少应支持：

- 从游戏日志中提取抽卡 URL
- 使用本地缓存参数刷新抽卡数据
- **手动输入从日志提取出来的抽卡 URL**

说明：

- “手动输入 URL”是本项目额外需求，不是参考仓库核心逻辑。
- 该功能应作为输入方式扩展实现。
- 它不能破坏核心抽卡分析流程。

### 3. 后续可扩展输入方式

未来可能增加：

- 手动补全抽卡记录
- 导入外部文件（如 Excel / CSV）
- 手动导入抽卡 JSON

这些属于扩展输入层，不应污染核心分析算法模块。

### 4. 图片资源要求

- 角色头像、武器图标优先走本地资源。
- 不要在抽卡列表渲染时对每个条目实时拉远程图片。
- 资源同步应作为独立模块处理。

---

## 推荐模块边界

建议按以下边界组织实现：

### Rust

- `config`：配置读取与校验
- `gacha_log`：日志扫描与 URL 提取
- `gacha_params`：URL 参数解析与转换
- `gacha_api`：抽卡接口请求
- `gacha_storage`：本地 JSON 存储
- `gacha_merge`：新旧记录合并
- `gacha_analysis`：抽卡统计
- `resource_sync`：资源同步
- `error`：统一错误定义
- `commands`：Tauri command 暴露层

### Frontend

- `routes`：页面
- `lib/components`：通用组件
- `lib/features/gacha`：抽卡分析前端功能
- `lib/stores`：状态管理
- `lib/api`：对 Tauri command 的封装
- `lib/types`：前后端共享的数据类型定义

---

## 调试要求

实现过程中优先保证：

- 出错时能知道是日志提取失败、参数解析失败、网络请求失败、文件读写失败，还是分析失败。
- 关键步骤要能看到输入输出摘要。
- 日志中不要泄露敏感信息，但应保留足够定位问题的上下文。

建议重点打日志的地方：

- 日志文件路径发现
- 抽卡 URL 提取结果
- URL 参数解析结果
- 接口请求目标服区
- 各卡池请求开始/结束
- 合并前后记录数量
- 分析前后统计摘要
- 资源同步成功/失败数量

---

## 禁止事项

- 不要把核心统计逻辑写死在页面组件里。
- 不要把所有功能都塞进单个 Tauri command。
- 不要为了省事把所有数据处理都放前端。
- 不要无说明地修改抽卡统计口径。
- 不要引入过重、与当前栈不匹配的框架来重做整个项目。

---

## 默认工作流程

当接到“实现抽卡分析”相关任务时，默认按以下顺序进行：

1. 先阅读 `AGENTS.md`
2. 再阅读 `doc/reference-gacha-resource-tech-note.md`
3. 输出技术实现路线或模块拆分
4. 确认后再逐步实现
5. 每完成一块后进行验证
6. 保持日志、注释和错误处理可用
